#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    // long long t, i;
    // cin >> t;
    // while (t--)
    // {
        
    // }
    int n;
    cin>>n;
    if(n%4==0 or n%7==0 or n%44==0 or n%47==0 or n%74==0 or n%77==0 or n%474==0 or n%444==0 or n%477==0 or n%774==0 or n%777==0 or n%744==0 or n%447==0 or n%747==0)
    {
        cout<<"YES"<<endl;
    }
    else
    cout<<"NO"<<endl;

    
}
